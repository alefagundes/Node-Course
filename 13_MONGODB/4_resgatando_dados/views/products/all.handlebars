<h1>Productos</h1>
<div class="products">
    {{#each products}}
    <div class="product">
        <img src="{{this.image}}" alt="{{this.name}}">
        <h3>{{this.name}}</h3>
        <p>{{this.price}}</p>
    </div>
    {{/each}}
</div>